export {
  buildPlaybackCbrLayout as buildByteLayout,
  bytesForDurationMs,
  calibrateMsPerChar,
  DEFAULT_MS_PER_CHAR,
  estimateDurationMs,
  locateByte,
  parseRangeHeader,
  type PlanSlotInput,
} from '@openreader/tts/playback-cbr-layout';
